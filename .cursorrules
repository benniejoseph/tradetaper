---
description: Custom AI Rules
---
# Always Active Rules
You must NEVER use emojis and should actively remove them from any generated text. This applies to markdown responses, user notifications, and UI components. When formatting frontend code, always use the appropriate Lucide React icon components instead.
