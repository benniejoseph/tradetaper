# TradeTaper Admin Remediation Backlog

Date: March 19, 2026  
Scope: `tradetaper-admin` + admin-facing backend contract in `tradetaper-backend`

## Execution Status (Updated: March 19, 2026)
- `TTADM-P0-01`: Completed
- `TTADM-P0-02`: Completed
- `TTADM-P0-03`: Completed
- `TTADM-P0-04`: Completed
- `TTADM-P0-05`: Completed
- `TTADM-P1-02`: Completed
- `TTADM-P1-03`: Completed
- `TTADM-P1-04`: Completed
- `TTADM-P1-01`: Completed (validator fixed; Supabase pooler discovery + enum-safe plan SQL shipped; live KPI tolerance check now passes)
- `TTADM-P2-01`: Completed (real observability endpoints shipped for logs/performance/API usage; system-health now DB/runtime-backed instead of constants)
- `TTADM-P2-02`: Completed (OpenAPI-backed generated contracts wired + warning-heavy pages migrated off broad `any`; admin lint now clean)
- `TTADM-P2-03`: Completed (live HTTP e2e suite now executes login+MFA + authenticated admin route contracts against DB-backed backend mode)
- `TTADM-P2-04`: Completed (scoped admin roles enforced in backend guard/routes and UI controls/nav gated by role)

## Objectives
- Eliminate production-risk behavior in admin console.
- Align frontend behavior to implemented backend endpoints.
- Harden auth/session and CSRF handling.
- Replace demo/synthetic operational views with real telemetry.
- Raise admin panel quality to production SaaS standard.

## Priority Model
- P0: must ship first, blocks trust/security/operability.
- P1: core correctness and operator productivity.
- P2: scale, maintainability, UX maturity.

## P0 (Week 1)

### TTADM-P0-01: Remove risky synthetic status probes
- Severity: Critical
- Problem: Status page generates fake login attempts and non-health checks.
- Files:
  - `tradetaper-admin/src/app/status/page.tsx`
- Tasks:
  - Remove `/admin/auth/login` probing with fake credentials.
  - Remove `/admin/database/tables` unauthorized probe.
  - Remove `/api/v1/ping` probe (endpoint does not exist).
  - Restrict to safe health checks only (`/api/v1/health`) until backend health aggregator exists.
- Acceptance:
  - No automated 401 spam from status screen.
  - No auth audit noise generated by status checks.
  - All checks map to real backend routes.
- Effort: 0.5 day

### TTADM-P0-02: Cookie-only admin session model
- Severity: High
- Problem: `admin_token` in `localStorage` increases XSS impact and duplicates auth state.
- Files:
  - `tradetaper-admin/src/lib/api.ts`
  - `tradetaper-admin/src/app/login/page.tsx`
- Tasks:
  - Remove token persistence to `localStorage`.
  - Remove Authorization header injection from `localStorage`.
  - Keep `withCredentials: true` and rely on HttpOnly cookie.
- Acceptance:
  - Login/logout works across all admin pages.
  - No `admin_token` stored in browser storage.
- Effort: 0.5 day

### TTADM-P0-03: CSRF alignment for admin mutating requests
- Severity: High
- Problem: backend CSRF is enabled in production; admin client does not send CSRF token.
- Files:
  - `tradetaper-admin/src/lib/api.ts`
  - `tradetaper-backend/src/common/controllers/csrf.controller.ts`
- Tasks:
  - Add CSRF bootstrap call (`GET /api/v1/csrf-token`) on app init/session start.
  - Add axios interceptor to include `X-CSRF-Token` on non-GET methods.
  - Add auto-refresh path when CSRF token missing/expired.
- Acceptance:
  - POST/DELETE/PATCH requests succeed in production with CSRF enabled.
  - No CSRF 403 for normal admin flows.
- Effort: 1 day

### TTADM-P0-04: Contract-lock frontend endpoints to backend reality
- Severity: Critical
- Problem: frontend calls many endpoints not implemented in backend.
- Files:
  - `tradetaper-admin/src/lib/api.ts`
  - `tradetaper-backend/src/admin/admin.controller.ts`
- Tasks:
  - Remove/flag dead API methods and unreachable UI actions.
  - Add capability map for currently supported admin endpoints.
  - Fail explicitly with “Not available in this environment” instead of silent fallback.
- Acceptance:
  - Every active admin action maps to a real backend route.
  - No runtime 404 from primary navigation flows.
- Effort: 1 day

### TTADM-P0-05: Hide non-production modules until backed by real data
- Severity: Medium
- Problem: `geographic`, `ui-lab`, and parts of `testing/status` are demo-like.
- Files:
  - `tradetaper-admin/src/components/Sidebar.tsx`
  - `tradetaper-admin/src/app/geographic/page.tsx`
  - `tradetaper-admin/src/components/WorldMap.tsx`
- Tasks:
  - Remove from default nav or add explicit `beta` gating.
  - Replace hardcoded filler copy with real-data state or “not enabled”.
- Acceptance:
  - No fake operational data visible in production routes.
- Effort: 0.5 day

## P1 (Week 2)

### TTADM-P1-01: KPI/data correctness pass
- Severity: High
- Areas:
  - plan normalization (`free`/`Free`/tier mapping),
  - verified user logic, active user definitions,
  - revenue/trade summary semantics.
- Files:
  - `tradetaper-admin/src/app/billing/page.tsx`
  - `tradetaper-admin/src/app/users/page.tsx`
  - `tradetaper-backend/src/admin/admin.service.ts`
- Acceptance:
  - KPI values match DB query outputs within agreed tolerance.
- Effort: 1.5 days
- Validation command:
  - `cd tradetaper-backend && npm run validate:admin-kpis`
- Latest run (March 19, 2026):
  - Validation now passes with zero KPI mismatches.
  - Root fixes:
    - Added default Supabase pooler host-prefix fallback (`aws-0` + `aws-1`) with optional CLI region hinting.
    - Switched validator datasource entity loading to full entity glob to prevent relation metadata failures during bootstrap.
    - Fixed enum-safe plan normalization SQL (`LOWER(sub.plan::text)` and `LOWER(sub.tier::text)`) in admin KPI queries.
  - Current effective DB path from this runner:
    - Direct DB host (`db.bzzdioswzlzvvlmellzh.supabase.co:5432`) remains IPv6-unreachable here.
    - Validator auto-falls back to working pooler route (`aws-1-ap-south-1.pooler.supabase.com:5432`, user `postgres.<project_ref>`), then completes successfully.

### TTADM-P1-02: Unify data fetching and auth error handling
- Severity: Medium
- Problem: mixed raw `fetch` + `adminApi` leads to inconsistent 401/redirect behavior.
- Files:
  - `tradetaper-admin/src/app/accounts/page.tsx`
  - `tradetaper-admin/src/app/users/page.tsx`
  - `tradetaper-admin/src/app/database/page.tsx`
- Acceptance:
  - All pages use one client abstraction for auth, CSRF, retries, and errors.
- Effort: 1 day

### TTADM-P1-03: Export hardening (CSV injection + escaping)
- Severity: Medium
- Files:
  - `tradetaper-admin/src/app/users/page.tsx`
  - `tradetaper-admin/src/app/trades/page.tsx`
  - `tradetaper-admin/src/app/accounts/page.tsx`
  - `tradetaper-admin/src/app/memberships/page.tsx`
- Acceptance:
  - CSV fields escaped correctly.
  - Formula-prefix values sanitized (`=`, `+`, `-`, `@`).
- Effort: 0.5 day

### TTADM-P1-04: Accessibility baseline
- Severity: Medium
- Focus:
  - keyboard/focus order,
  - aria labels for icon-only controls,
  - color contrast and error messaging.
- Files:
  - `tradetaper-admin/src/app/login/page.tsx`
  - `tradetaper-admin/src/components/Sidebar.tsx`
  - major pages under `src/app/**/page.tsx`
- Acceptance:
  - Keyboard navigation works on all primary flows.
  - No critical WCAG contrast/focus violations in core routes.
- Effort: 1 day

## P2 (Weeks 3-4)

### TTADM-P2-01: Real backend admin observability endpoints
- Add supported endpoints for logs, performance, and usage OR remove UI permanently.
- Backed by real sources (DB/system metrics), not constants.
- Progress (March 19, 2026):
  - Added backend routes:
    - `GET /admin/logs`
    - `GET /admin/logs/stream`
    - `GET /admin/analytics/performance`
    - `GET /admin/api/usage`
  - Added runtime request telemetry capture in `LoggingInterceptor` via `RequestMetricsStore`.
  - Replaced static `getSystemHealth()` constants with runtime + DB-backed probes:
    - DB ping latency (`SELECT 1`)
    - DB connections (`pg_stat_activity`)
    - cache hit ratio (`pg_stat_database`)
    - process memory/cpu/disk metrics
    - request-derived error/api call counts from live telemetry
  - Updated OpenAPI contract + generated admin DTOs for all observability endpoints.
  - Frontend `adminApi` now consumes live observability routes (no feature-unavailable stubs for these paths).

### TTADM-P2-02: Typed API contracts
- Generate shared admin DTO types (OpenAPI or typed client generation).
- Eliminate broad `any` usage in admin frontend.
- Progress (March 19, 2026):
  - Added typed admin DTO contracts in `tradetaper-admin/src/lib/api.ts` for users/trades/accounts/subscriptions.
  - Migrated `users`, `trades`, `accounts`, `memberships` pages to consume typed responses (removed broad `any` in these routes).
  - Added OpenAPI spec at `tradetaper-admin/openapi/admin-contracts.yaml`.
  - Added contract generation script: `npm run generate:admin-contracts`.
  - Generated contracts at `tradetaper-admin/src/lib/generated/admin-contracts.ts` and switched core DTOs in `src/lib/api.ts` to generated schema aliases.
  - Removed warning-heavy `any` usage in remaining scoped routes: `activity`, `database`, `logs`, `testing`, and dashboard analytics blocks in `src/app/page.tsx`.
  - Cleared final `no-explicit-any` lint warning in `src/app/ui-lab/page.tsx`; `npm run lint` now returns zero warnings/errors.

### TTADM-P2-03: Admin test suite
- Contract tests for all admin routes.
- E2E smoke tests for login (incl MFA), dashboard, users, trades, DB viewer.
- Progress (March 19, 2026):
  - Added OpenAPI-backed admin contract test scaffold: `tradetaper-backend/test/admin-contract.e2e-spec.ts`.
  - Added backend test command: `npm run test:admin-contract`.
  - Added schema validation tooling for contract checks (`ajv`, `ajv-formats`, `@apidevtools/swagger-parser`, `@openapi-contrib/openapi-schema-to-json-schema`).
  - Current scaffold validates core admin route response shapes against `tradetaper-admin/openapi/admin-contracts.yaml` using controller-level fixtures (no external DB dependency).
  - Added live HTTP e2e suite: `tradetaper-backend/test/admin-live-http.e2e-spec.ts`.
  - Added backend command: `npm run test:admin-live-http`.
  - Live suite scope:
    - Full admin login flow with MFA challenge handling (OTP/recovery and optional bootstrap).
    - Authenticated live route contract checks for dashboard, analytics, users, trades, accounts, subscriptions, audit logs, and DB viewer endpoints.
  - Added in-process DB-backed execution mode (`ADMIN_E2E_INPROCESS=true`) to run full HTTP admin e2e when direct deployed-admin credentials are unavailable.
  - Executed successfully on March 19, 2026 in DB-backed mode:
    - `ADMIN_E2E_LIVE=true ADMIN_E2E_INPROCESS=true npm run test:admin-live-http`
    - Result: 3/3 tests passed (login+MFA, route contract checks, logout).
  - Fixed runtime DTO drift uncovered by live suite:
    - `GET /admin/trades` decimal fields were serialized as strings from DB (`openPrice`, `closePrice`, `profitOrLoss`) and now normalized to numeric responses in `AdminService`.
    - Applied same numeric normalization to user-detail trades, accounts balances, and subscription prices for contract consistency.
  - Live suite gating:
    - Disabled by default; enable with `ADMIN_E2E_LIVE=true`.
    - Requires `ADMIN_E2E_PASSWORD` and `ADMIN_E2E_EMAIL` (or `ADMIN_EMAIL`).
    - MFA env knobs: `ADMIN_E2E_EXPECT_MFA`, `ADMIN_E2E_TOTP_SECRET_BASE32`, `ADMIN_E2E_RECOVERY_CODE`, `ADMIN_E2E_ALLOW_BOOTSTRAP`.

### TTADM-P2-04: Role-based admin capabilities
- Introduce scoped admin roles (super-admin, billing-admin, readonly-ops).
- Enforce permissions in backend and hide/disable disallowed UI controls.
- Progress (March 19, 2026):
  - Added admin role metadata/decorator support:
    - `@AdminRoles(...)` + `AdminGuard` role checks.
    - JWT payload now carries `adminRole`.
    - Role resolution via `ADMIN_ROLE` default + optional `ADMIN_ROLE_BINDINGS` email map.
  - Added `GET /admin/auth/me` session endpoint exposing `email`, `role`, `adminRole`, and MFA status for UI gating.
  - Enforced backend route permissions:
    - `super-admin` only: dangerous mutations (`seed-sample-data`, test-user create/delete, DB clear, run-sql).
    - `super-admin` + `readonly-ops`: database read endpoints (tables/columns/rows/table-stats).
  - UI role gating implemented:
    - Sidebar hides `Billing` and `Database` entries when disallowed.
    - Billing page blocks non-billing roles.
    - Database page blocks disallowed roles and disables SQL runner for non-super-admin roles.

## Delivery Sequence
1. P0-01, P0-02, P0-04 (stabilize and remove dangerous drift).
2. P0-03 (make mutating ops production-safe).
3. P0-05 + P1-02 (cleanup operator experience).
4. P1-01, P1-03, P1-04 (correctness + quality).
5. P2 (hardening and scale path).

## Definition of Done (Admin Reliability Gate)
- Zero known dead endpoint calls in primary routes.
- No auth spam generated by UI telemetry screens.
- Cookie + CSRF flows verified in production-like environment.
- KPI values validated against backend query outputs.
- Lint warnings materially reduced (especially `any` and unused symbols).
- Core E2E smoke suite green.

## Immediate Start Pack (Next 48 Hours)
1. Implement P0-01 and P0-02.
2. Ship P0-04 contract cleanup for active nav routes.
3. Deploy admin and verify with manual smoke:
   - login + MFA
   - dashboard load
   - users/trades/accounts/memberships pages
   - DB read-only viewer
